# Worked examples — Phase 6

**Currently empty.** Phase 6 deliverable.

When populated:

- `discover-bio-student.md` — fixture profile (AP Bio + Python + school lab + 14 weeks); expected output structure
- `discover-cs-student.md` — CS-leaning profile; light depth example
- `discover-novice-offramp.md` — profile that triggers the readiness off-ramp (T2.3 < 5)
- `score-phys021.md` — golden run on the 2026 PHYS grand-award contender; per-dimension transparency required (calibration anchor for A8)
- `score-saturated-topic.md` — "Train ResNet to classify chest X-rays on Kaggle dataset"; must score in the low range with explicit saturation callout

Each example is a Markdown file containing:
1. The student profile / input topic
2. The `--lang` and `--depth` settings used
3. The expected sequence of skill turns
4. The final rendered output (or a representative excerpt)

These are NOT live integration tests; they document expected behavior for human reviewers and serve as fixtures for back-testing.
